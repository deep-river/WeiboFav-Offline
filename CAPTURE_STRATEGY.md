# 批量采集策略

## 已验证

- 原微博正文、作者、发布时间和原帖链接可从正文页读取；评论不入库。
- 转发微博将“转发说明”和“原微博”作为两层保存，阅读时优先展示原微博。
- 图片提供 `orj360` / `orj480` 地址，已实际保存 3 张原图。
- 视频不下载 MP4；仅保存正文页的视频首帧图，已实际保存 1 张缩略图。
- 已删除内容记录为 `skipped_deleted`，不重试，不创建内容记录。

## 温和、可续跑的运行方式

1. 脚本顺序解析收藏列表页，只取得正文链接与收藏上下文；每页发现结果立即写入 SQLite 队列和观察记录，重复运行时按 URL 去重。
2. 常规采集使用无界面、专用且已登录的 Chrome 配置目录；首次登录才显示窗口。脚本顺序请求微博自身的状态数据，转发微博额外请求原微博详情以取得完整正文、图集或视频封面。默认每个请求随机等待 3.5–6 秒；正文不并发，同一条微博最多并发下载 2 个媒体文件。
3. 网络和下载错误最多自动重试 3 次；已删除内容自动标记为 `skipped_deleted`。连续两条未恢复错误才停止，已完成记录和失败队列可由 `--resume` 自动续跑。
4. 媒体先写入临时文件，校验图像签名、长度、SHA-256 和可用空间后原子移动到 `<dataDir>/media/<微博ID>/`；视频只取首帧/封面。只有完整媒体清单成功写入后，整条微博才标记为已采集。
5. 归档后的 URL 是不可变快照：常规增量采集只追加新 URL，绝不覆盖已有正文或媒体。本地删除会写入独立删除标记，后续即使线上仍收藏也不会重新导入；线上取消收藏不会删除本地备份。失败任务仅通过 `--resume` 重试。

## 增量与本地删除

- 运行 `pnpm capture -- --incremental` 会从第 1 页开始扫描；连续 3 个完整页面没有发现新 URL 时结束。可用 `--known-pages 5` 或 `--max-pages 200` 调整边界。
- `favorite_observations` 记录每个 URL 首次和最后一次出现的时间、页码及观察次数，只用于判断增量水位和审计线上变化。
- `local_tombstones` 保存本地删除的 URL 与微博 ID。它独立于抓取队列，因此删除过的内容不会因后续网页变化而复活。

## 容量与本地库

- 始终预留 10 GB 或可用空间的 10%（取较大值）。图片只下载原图，视频只允许缩略图；流式下载达到配额立即取消。
- `<dataDir>/library.sqlite3` 保存元数据；`<dataDir>/media/` 按微博 ID 存放媒体。默认 `dataDir` 是项目中的 `data/`，可通过 `capture.config.json` 的 `dataDir` 或 `WEIBOFAV_DATA_DIR` 指向项目外的目录。
- 页面按原微博发布时间展示，支持全文检索、图片/视频筛选、多选和永久删除；删除同时移除数据库记录和对应媒体文件。
